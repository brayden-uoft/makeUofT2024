// This detects when a page is loaded and extracts the URL.
// It then calls the homographs checker function in homographs.json
// If it detects a homograph, display a warning pop-up and generate a safety report